{
  "name": "@js/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared types for the job scheduler monorepo",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'lint ok'"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
